<!-- CTA / Contact Section -->
<section id="contacto" class="py-24 relative">
    <div class="max-w-4xl mx-auto px-4">
        <div
            class="glass rounded-3xl p-8 md:p-16 text-center relative overflow-hidden reveal"
        >
            <!-- Glow effect inside card -->
            <div
                class="absolute top-0 left-1/2 transform -translate-x-1/2 w-full h-1 bg-gradient-to-r from-transparent via-cyan-500 to-transparent opacity-50"
            >
            </div>

            <h2 class="text-3xl md:text-5xl font-bold mb-6">
                ¿Listo para escalar?
            </h2>
            <p class="text-gray-300 mb-10 max-w-lg mx-auto">
                Déjanos tus datos. Analizamos tu situación actual y nos ponemos
                en contacto para ofrecerte una solución a medida.
            </p>

            <form
                name="contact"
                method="POST"
                data-netlify="true"
                netlify-honeypot="bot-field"
                class="space-y-4 max-w-md mx-auto text-left"
            >
                <!-- Campo oculto para Netlify -->
                <input type="hidden" name="form-name" value="contact" />

                <!-- Campo honeypot para prevenir spam -->
                <p class="hidden">
                    <label>
                        Don't fill this out if you're human: <input
                            name="bot-field"
                        />
                    </label>
                </p>

                <div>
                    <label
                        for="nombre"
                        class="block text-xs uppercase tracking-wider text-gray-400 mb-1"
                        >Nombre</label
                    >
                    <input
                        type="text"
                        id="nombre"
                        name="nombre"
                        required
                        class="w-full bg-black/40 border border-gray-700 rounded-lg px-4 py-3 text-white focus:outline-none focus:border-cyan-500 transition-colors"
                    />
                </div>
                <div>
                    <label
                        for="email"
                        class="block text-xs uppercase tracking-wider text-gray-400 mb-1"
                        >Email</label
                    >
                    <input
                        type="email"
                        id="email"
                        name="email"
                        required
                        class="w-full bg-black/40 border border-gray-700 rounded-lg px-4 py-3 text-white focus:outline-none focus:border-cyan-500 transition-colors"
                    />
                </div>
                <div>
                    <label
                        for="mensaje"
                        class="block text-xs uppercase tracking-wider text-gray-400 mb-1"
                        >Tu desafío actual</label
                    >
                    <textarea
                        id="mensaje"
                        name="mensaje"
                        rows="3"
                        required
                        class="w-full bg-black/40 border border-gray-700 rounded-lg px-4 py-3 text-white focus:outline-none focus:border-cyan-500 transition-colors"
                    ></textarea>
                </div>
                <button
                    type="submit"
                    class="w-full bg-white text-black font-bold py-4 rounded-lg hover:bg-cyan-400 transition-colors shadow-[0_0_20px_rgba(255,255,255,0.3)] hover:shadow-[0_0_30px_rgba(6,182,212,0.6)]"
                >
                    SOLICITAR ASESORÍA
                </button>
            </form>

            <p class="mt-6 text-xs text-gray-500">
                Tu información está segura. Solo la usamos para contactarte.
            </p>
        </div>
    </div>
</section>
